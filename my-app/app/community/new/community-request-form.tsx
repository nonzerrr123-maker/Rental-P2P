"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { Button } from "@/components/ui/button";
import { FormMessage } from "@/components/ui/form-field";
import { Select } from "@/components/ui/select";
import { Textarea } from "@/components/ui/textarea";
import { communityRequestFormSchema, type CommunityRequestFormInput } from "@/lib/forms/rental-actions";

const inputClass="mt-2 min-h-12";
export default function CommunityRequestForm(){
 const router=useRouter();const[locating,setLocating]=useState(false);const[message,setMessage]=useState("");const[serverErrors,setServerErrors]=useState<Record<string,string>>({});const[coords,setCoords]=useState({latitude:"",longitude:""});
 const{register,handleSubmit,formState:{errors,isSubmitting}}=useForm<CommunityRequestFormInput>({resolver:zodResolver(communityRequestFormSchema),defaultValues:{title:"",category:"",description:"",targetPrice:"",neededStartsAt:"",neededEndsAt:"",province:"",district:"",subdistrict:"",isUrgent:false}});
 const useCurrentLocation=()=>{if(!navigator.geolocation){setMessage("เบราว์เซอร์นี้ไม่รองรับ Location — กรอกพื้นที่ด้วยตนเองได้");return;}setLocating(true);setMessage("");navigator.geolocation.getCurrentPosition((position)=>{setCoords({latitude:position.coords.latitude.toFixed(6),longitude:position.coords.longitude.toFixed(6)});setLocating(false);},()=>{setMessage("อ่านตำแหน่งไม่ได้ คุณยังโพสต์ได้โดยใช้จังหวัด/อำเภอ/ตำบล");setLocating(false);},{enableHighAccuracy:false,timeout:10_000,maximumAge:60_000});};
 const submit=handleSubmit(async(values)=>{setMessage("");setServerErrors({});try{const response=await fetch("/api/community-requests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...values,latitude:coords.latitude,longitude:coords.longitude,neededStartsAt:new Date(values.neededStartsAt).toISOString(),neededEndsAt:new Date(values.neededEndsAt).toISOString()})});const result=await response.json();if(!response.ok||!result.ok){if(result.fieldErrors&&typeof result.fieldErrors==="object")setServerErrors(result.fieldErrors);setMessage(result.message??"สร้างคำขอไม่สำเร็จ");return;}router.push(`/community/${result.request.id}`);router.refresh();}catch{setMessage("เชื่อมต่อระบบคอมมูหาของไม่สำเร็จ กรุณาลองอีกครั้ง");}});
 const fieldError=(name:keyof CommunityRequestFormInput)=>errors[name]?.message||serverErrors[name];
 return <form onSubmit={submit} className="mt-8 rounded-3xl border border-neutral-200 bg-white p-5 shadow-sm md:p-8" noValidate><div className="grid gap-5"><label className="text-sm font-bold">ของที่กำลังหา<input {...register("title")} maxLength={120} placeholder="เช่น โปรเจคเตอร์ Full HD สำหรับพรุ่งนี้" className={`bb-input ${inputClass}`}/><FormMessage>{fieldError("title")}</FormMessage></label><div className="grid gap-5 md:grid-cols-2"><label className="text-sm font-bold">หมวดหมู่<Select {...register("category")} className={inputClass}><option value="">เลือกหมวดหมู่</option>{["อิเล็กทรอนิกส์","กล้องและอุปกรณ์ถ่ายภาพ","เกม","แคมป์ปิ้ง","เครื่องมือ","กีฬา","ยานพาหนะและอุปกรณ์","อื่น ๆ"].map(v=><option key={v}>{v}</option>)}</Select><FormMessage>{fieldError("category")}</FormMessage></label><label className="text-sm font-bold">งบเป้าหมาย<input {...register("targetPrice")} type="number" min="0" max="10000000" step="0.01" placeholder="500" className={`bb-input ${inputClass}`}/><FormMessage>{fieldError("targetPrice")}</FormMessage></label></div><label className="text-sm font-bold">รายละเอียด<Textarea {...register("description")} rows={5} maxLength={3000} placeholder="บอกสเปก ของที่ต้องมี การใช้งาน หรือเงื่อนไขที่สำคัญ" className="mt-2"/><FormMessage>{fieldError("description")}</FormMessage></label><section className="rounded-2xl bg-neutral-50 p-4 md:p-5"><h2 className="font-black">ช่วงเวลาที่ต้องการ</h2><div className="mt-3 grid gap-4 md:grid-cols-2"><label className="text-sm font-bold">เริ่มใช้<input {...register("neededStartsAt")} type="datetime-local" className={`bb-input ${inputClass}`}/><FormMessage>{fieldError("neededStartsAt")}</FormMessage></label><label className="text-sm font-bold">คืน / สิ้นสุด<input {...register("neededEndsAt")} type="datetime-local" className={`bb-input ${inputClass}`}/><FormMessage>{fieldError("neededEndsAt")}</FormMessage></label></div></section><section className="rounded-2xl bg-neutral-50 p-4 md:p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-black">พื้นที่ที่สะดวกรับของ</h2><p className="mt-1 text-xs text-neutral-500">พิกัดใช้ค้นหาระยะทางเท่านั้น และไม่เปิดต่อสาธารณะ</p></div><Button type="button" variant="outline" disabled={locating} onClick={useCurrentLocation}>{locating?"กำลังอ่านตำแหน่ง...":"📍 ใช้ตำแหน่งปัจจุบัน"}</Button></div><div className="mt-3 grid gap-4 md:grid-cols-3"><label className="text-sm font-bold">จังหวัด<input {...register("province")} placeholder="ชลบุรี" className={`bb-input ${inputClass}`}/><FormMessage>{fieldError("province")}</FormMessage></label><label className="text-sm font-bold">อำเภอ / เขต<input {...register("district")} className={`bb-input ${inputClass}`}/></label><label className="text-sm font-bold">ตำบล / แขวง<input {...register("subdistrict")} className={`bb-input ${inputClass}`}/></label></div>{coords.latitude&&<p className="mt-3 text-xs font-semibold text-green-700">✓ บันทึกจุดอ้างอิงสำหรับค้นหาระยะทางแล้ว</p>}</section><label className="flex cursor-pointer items-start gap-3 rounded-2xl border border-[#c9a227]/40 bg-[#fffaf0] p-4"><input {...register("isUrgent")} type="checkbox" className="mt-1 h-5 w-5 accent-[#c9a227]"/><span><b>⚡ ต้องการด่วน</b><span className="mt-1 block text-xs text-neutral-600">ดันขึ้นในตัวกรองด่วน แต่ Checkout/Payment ยังใช้ flow ปกติ</span></span></label>{message&&<p role="alert" className="rounded-xl bg-red-50 p-4 text-sm font-semibold text-red-700">{message}</p>}<div className="flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><Link href="/community" className="rounded-xl border px-5 py-3 text-center text-sm font-bold">ยกเลิก</Link><Button type="submit" disabled={isSubmitting}>{isSubmitting?"กำลังโพสต์...":"โพสต์คำขอหาของ"}</Button></div></div></form>;
}
